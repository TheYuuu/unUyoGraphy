import * as React from "react";

export default class preview extends React.Component<{}> {
  constructor(props: {}) {
    super(props);
  }

  render() {
    return (
      <div>
        preview
      </div>
    );
  }
}